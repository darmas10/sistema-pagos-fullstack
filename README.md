this proyect contain a full stack proyect about System Pay , for runing the backend execute mvn spring-boot:runs ,  for runing the frontend execute ng server --open 
required:
install angular cli and the dependencies in the package loock.json
install maven and java jdk 17 also needed to configure the path and JAVA_HOME in the evironment var system   
for test the enpoints use this URL http://localhost:8081/swagger-ui/index.html#/ 
